## 2026-07-21
- 产出 docs/KYLECLAUDE_VS_MAINSTREAM_AGENT_ARCH.md：把 KyleClaude 与主流 Agent 架构（Coding-Loop / Graph-State / 对话型多Agent / Handoff-SDK / Durable orchestration）做结构性对比，识别五大断层（无第二控制流、运行态不可恢复、工具能力模型缺失、人机协作只有权限层、Trace 是流非图），给出 A-F 六阶段改造方向。
- 落地 Phase A1 工具能力模型（与 KYLECLAUDE_VS_CLAUDE_CODE_GAP_ANALYSIS P0-2 互补）：
  - base.py 引入 ToolSideEffect 枚举（NONE/LOCAL_WRITE/EXTERNAL_WRITE）、side_effect ClassVar 默认 EXTERNAL_WRITE（保守）、can_parallel ClassVar 默认 False、is_read_only 属性。retry_policy 早已存在并按 tool.can_retry() 工程正确行为，未触动 invocation.py。
  - 19 个内置工具按本性显式标注：纯读（read_file/glob/grep/list_dir/git_diff/checkpoint_list/task_list/task_get/memory_search/background_result/background_list/worktree_list/agent_result）→ NONE+True；本地写（edit_file/write_file/apply_patch/checkpoint_rewind）→ LOCAL_WRITE；外部写沿用默认 EXTERNAL_WRITE（含 bash/spawn_agent/worktree_create/remove/background_start/cancel/task_create/claim/update/memory_save/forget/mcp 等）。
  - agents/loader.py AgentProfile 新增 restrict 字段（取值 read_only），由 TOML 解析。
  - subagent/tool.py _build_child_registry 改造：当 profile.restrict=="read_only" 时按工具 is_read_only 过滤；显式 allowed_tools 与 restrict 同时存在时从严取子集；保留向后兼容（restrict 空且 allowed_tools 空时不过滤）。
  - reviewer.toml：删 allowed_tools 中含 bash（违反"只读"提示的安全 bug），改 restrict = "read_only"。
  - 新增 tests/unit/test_tool_capability.py 7 测覆盖默认值、capability 分类、reviewer restrict 排除写工具、restrict 与 allowed_tools 取严子集、allowed_tools 单独生效、profile=None 不过滤；更新 test_agent_profile_loader.py 适应 reviewer 不再列 allowed_tools。
  - ruff/mypy clean；pytest 全量 400 passed + 2 skipped + 1 unrelated IPC symlink failure（同改造前基线一致）；集成测试 14 passed；import 冒烟通过。
- Phase A2（loop 消费 can_parallel，切批并发执行）落地：
  - 仅改 src/kyle_claude/core/loop.py：把原 [act] 段 for 循环抽出为 _run_act_phase，按"连续 can_parallel=True 工具组成一个 asyncio.gather 批，副作用/未知工具单独串行"切批。
  - 加 4 个私有 helper：_is_parallelable(tool_call)、_invoke_one(tc, ctx)、_record_result(idx, ..., tc, result, ctx) -> bool(命中 permission_required?)、_run_act_phase(tool_calls, ctx)。permission_required 仍立即 mark_failed 并 stop；context.add_tool_result / transcript.append_tool_result 的 block_index/block_count 仍按模型原顺序回填。
  - 关键决策：同大批中一个工具异常不取消同批其它工具（已发出的 read 不丢），结果按 tool_use_id 顺序回填；未 batchSize>1 才用 gather，避免单工具有额外开销（gather 单 await 性能更差）。
  - 新增 tests/unit/test_loop_parallel.py 5 测：(1) 三独立 async_read 真并发（elapsed < 0.10s 且 max(starts) < min(ends)）；(2) [read, edit, read] 顺序三批串行（spread >= 0.14s）；(3) 批中一 fail_read 不阻断同批两个 ok_read，context 收到按 tool_use_id 顺序的三个 tool_result，fail 那个 is_error=True；(4) 未知工具走单独串行分支返回 runtime_error；(5) 单批单工具仍工作。
  - ruff check src tests scripts 全绿；mypy src 122 files 0 errors；pytest tests/unit 405 passed + 2 skipped + 1 unrelated IPC symlink failure（与改造前基线一致）；pytest tests/integration 14 passed + 1 skipped；导入冒烟验证 _run_act_phase 已注册、ReadFileTool.can_parallel=True。
- Phase A3（TodoState 软状态机：task_* 晋升为驱动 loop 的软状态机）落地：
  - task/manager.py 新增 TodoStateView Protocol（active_summary() / has_incomplete()），TaskManager 实现两方法（active_summary 返回 "## Todo State\n[ ]#1:x\n[>]#2:y" 格式；has_incomplete 以 any(t.status!="completed")）。
  - loop.py 新增 todo_state 可选参数（默认 None），增量设计：无 todo_state 时行为与改造前无差异。
  - 每个 LLM 调用 system prompt 通过 _render_system(context) 生成：base 系统提示后追加 todos 摘要（"## Todo State" 清单）。
  - end_turn 终止逻辑改造：若 todo_state 存在且 has_incomplete() 且 snapshot 与上轮不同，注入 reminder "You ended the turn, but the Todo State above still has incomplete items. Either continue working or call task_update..." 并推送为 user 消息；连续 _MAX_TODO_DEFERS(3) 次 todos 快照不变则放弃阻拦、正常结束（防死循环）。
  - runner.py AgentLoop 实例化时传 todo_state=task_manager；subagent/tool.py 不传（子 Agent 不受父级 todo 阻拦）。
  - 新增 tests/unit/test_loop_todo_state.py 8 测：(1) todo_state=None 不注入摘要；(2) 有 todos 时 system 含 "## Todo State"；(3) 空 TaskManager 不注入；(4) pending 时 end_turn 注入 reminder 一次后因 snapshot 不变放过；(5) 已完成 todos 时 end_turn 直接结束；(6) 连续 N 次无变化后停止阻拦；(7) 模型工具标完成再 end_turn 不阻拦；(8) TaskManager 满足 TodoStateView Protocol。
  - ruff check 全绿；mypy src 122 files 0 errors；pytest tests/unit 413 passed+2 skipped+1 unrelated IPC symlink failure；集成测试 14 passed+1 skipped；冒烟验证 TaskManager.has_incomplete()/active_summary() 正确 + AgentLoop 能带 todo_state 实例化。
- Phase A（工具能力模型 + 并行执行 + TodoState）整体完成。下一步为 Phase B（多 Agent 协作：AgentProfile 真接线 / handoff / daemon 级 TaskBoard）或 Phase C（Durable Run：RunStateStore + 增量落盘）。
- Phase B (B1+B3+B4) 落地：
  - B1 — AgentProfile model 真正接线：LLMProvider Protocol 加 model kwarg；AnthropicProvider / OpenAICompatibleProvider / TracingProvider 实现 model 参数（None 时用默认 model）；SpawnAgentTool.invoke() 在 child loop 构造处若 profile.model 非空包装 _ModelOverrideProvider 注入 model kwarg。reviewer/executor/planner 三个内建 profile 均有 model="claude-sonnet-4-6"，不再形同虚设。
  - B3 — 同 run 内共享 TaskManager：SpawnAgentTool 接受可选 task_manager 参数；invoke 中 self._task_manager 或 fallback 新建；_build_child_registry 接受 task_manager 参数替代局部 new；递归 SpawnAgentTool 也传递 task_manager；runner.py _build_registry 根 spawn tool 传入 task_manager。
  - B4 — BackgroundTaskRegistry 从 Runner 级提升到 daemon 级：CoreApp 持有 _subagent_registry=BackgroundTaskRegistry()；AgentRunner.__init__ 加 subagent_registry 可选参数（未传入时自己 new，向后兼容）；CoreApp runner_factory 传 subagent_registry；删除 runner 末尾 cancel_descendants 自动杀线；CoreApp shutdown 中 subagent_registry.cancel_all()。
  - 新增 tests/unit/test_phase_b.py 7 测覆盖以上行为。
  - ruff/mypy clean；pytest 420 passed+2 skipped+1 unrelated IPC symlink；集成 14 passed+1 skipped；import 冒烟确认 CoreApp._subagent_registry / AgentRunner 接收 subagent_registry / reviewer model=claude-sonnet-4-6。
- Phase B 下一步待做：B2（Handoff 工具，工作量最大 3-4 天，需新增 agent_handoff 命令+事件+loop 终止+session 所有权转移）。
